pipeline {
    agent any


    stages {
        stage('Build') {
            steps {
                echo 'branch ' + ${GIT_BRANCH}
                sh 'git checkout master'
                sh 'printenv'
            }
        }
    }
}
