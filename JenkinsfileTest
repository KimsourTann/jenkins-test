pipeline {
    agent any


    stages {
        stage('Build') {
            steps {
                sh 'branch ${GIT_BRANCH}'
                sh 'git checkout master'
                sh 'printenv'
            }
        }
    }
}
