pipeline {
    agent any


    stages {
        stage('Build') {
            steps {
               
                sh 'git checkout master'
                 sh '''git checkout ${GIT_BRANCH}'''
                sh 'printenv'
            }
        }
    }
}
