pipeline {
    agent any


    stages {
        stage('Build') {
            steps {
                echo "${GIT_BRANCH}"
                sh 'git checkout master'
                sh 'printenv'
            }
        }
    }
}
