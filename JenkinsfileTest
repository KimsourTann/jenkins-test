String getRepoName(){
    return scm.getUserRemoteConfigs()[0].getUrl().tokenize('/')[3].split("\\.")[0]
}
pipeline {
    agent any


    stages {
        stage('Build') {
            steps {
               
                echo getRepoName()
            }
        }
    }
}
